name: Hello WeaGo Action

on:
  push:
    branches:
      - main

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: Say hello
        run: echo "👋 Hello from GitHub Actions – WeaGo is alive!"

